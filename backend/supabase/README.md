# Migrations do backend

SQL de schema e seeds do Postgres (Supabase). Nomes de tabelas e colunas em **português**.

Aplicar no projeto remoto via Supabase Dashboard, MCP ou:

```bash
supabase db push
```

(quando o CLI estiver configurado com `supabase link` a partir de `backend/`)
